requires-atopile: ^0.15.0

builds:
  default:
    entry: elec/src/bedpresence.ato:Bedpresence
# Dependencies are vendored directly in elec/src/ and resolve via the
# compiler's source search path, so no dependency declarations are needed.
dependencies:
- type: registry
  identifier: atopile/mounting-holes
  release: 0.2.0
